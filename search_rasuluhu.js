const fs = require('fs');
async function test() {
  // Let's use tsx or just write a script that parses the JSON quran text
  // We need the data from wherever it is fetched.
  // Wait, we don't have quran-simple-clean.json locally in /data ?
  // Where is it?
}
test();
